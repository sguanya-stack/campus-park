{
  "name": "campus-park-app",
  "version": "1.0.0",
  "private": true,
  "description": "Campus parking app with Node.js backend",
  "scripts": {
    "seed": "node seed.js",
    "prestart": "node scripts/kill-port.js",
    "start": "NODE_OPTIONS=--dns-result-order=ipv4first node server.js",
    "dev:server": "PORT=5000 nodemon server.js",
    "dev:client": "http-server -p 3000 -c-1 .",
    "dev": "concurrently -k -n server,client -c green,blue \"npm run dev:server\" \"npm run dev:client\"",
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:seed": "node prisma/seed.js",
    "test:concurrency": "node test-concurrency.js"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "csv-parser": "^3.2.0",
    "dotenv": "^16.4.7",
    "fs-extra": "^11.3.4",
    "leaflet": "^1.9.4",
    "node-cron": "^4.2.1",
    "prisma": "^5.22.0",
    "react-leaflet": "^5.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "http-server": "^14.2.0",
    "nodemon": "^3.1.10"
  }
}

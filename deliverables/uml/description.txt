The system starts in a browser-based single-page application, where app.js handles routing, filtering, map rendering, and reservation workflows, then calls server.js through REST APIs for authentication, parking, booking, and statistics. The backend uses Prisma to access PostgreSQL, while scheduled jobs update parking availability and the PWA and map layers support installation, visualization, and navigation.
